<% if (page.prev || page.next) { %>
  <nav class="pagination">
    <% if (page.prev) { %>
      <a class="button button-secondary" href="<%- url_for(page.prev_link) %>">上一页</a>
    <% } %>
    <% if (page.next) { %>
      <a class="button button-secondary" href="<%- url_for(page.next_link) %>">下一页</a>
    <% } %>
  </nav>
<% } %>
